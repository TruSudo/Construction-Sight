schema_version = "constructionsight.network-contract/v1"
contract_id = "construction-sight-network-2026-07-15"
production_scheduling_authorized = false
concurrent_live_integration_authorized = false

[[policies]]
id = "CS-NET-001"
module = "constructionsight.ceqanet_discovery_http"
capability_id = "CS-CAP-005"
methods = ["GET"]
hosts = ["ceqanet.lci.ca.gov"]
path_prefixes = ["/Search/Advanced"]
query_scope = "none"
body_scope = "none"
connect_timeout_seconds = 5.0
read_timeout_seconds = 20.0
write_timeout_seconds = 5.0
pool_timeout_seconds = 5.0
max_response_bytes = 50000
redirect_policy = "deny"
media_types = ["text/html"]
encodings = ["utf-8", "windows-1252"]
retryable_failures = []
terminal_failures = ["transport failure", "timeout", "redirect", "access control", "rate limit", "malformed response", "oversized response", "media type drift", "encoding drift"]
max_attempts = 1
backoff_seconds = 0.0
jitter = "none"
idempotency_rule = "single read-only request"
rate_limit = "one operator-triggered discovery request"
concurrency_limit = 1
circuit_breaker = "not applicable: one attempt"
bulkhead = "one in-flight request"
cancellation = "operator cancellation terminates without result"
partial_result = "not permitted"
evidence_retention = "status, final URL, response classification, and bounded exact response bytes"
telemetry = "typed outcome without exception swallowing"
credential_authority = "none"
robots_terms_authority = "lawful public access preflight required"
access_control_behavior = "terminal; do not bypass"
no_bypass = true

[[policies]]
id = "CS-NET-002"
module = "constructionsight.adapters.ceqanet_listing_executor"
capability_id = "CS-CAP-005"
methods = ["GET"]
hosts = ["ceqanet.lci.ca.gov"]
path_prefixes = ["/Search"]
query_scope = "exact planner-generated query only"
body_scope = "none"
connect_timeout_seconds = 5.0
read_timeout_seconds = 20.0
write_timeout_seconds = 5.0
pool_timeout_seconds = 5.0
max_response_bytes = 50000
redirect_policy = "deny"
media_types = ["text/html"]
encodings = ["utf-8"]
retryable_failures = []
terminal_failures = ["transport failure", "timeout", "redirect", "access control", "malformed response", "oversized response", "source drift"]
max_attempts = 1
backoff_seconds = 0.0
jitter = "none"
idempotency_rule = "read-only page request bound to one plan"
rate_limit = "bounded by immutable plan"
concurrency_limit = 1
circuit_breaker = "not applicable: bounded manual execution"
bulkhead = "one in-flight request"
cancellation = "operator cancellation is distinct from empty results"
partial_result = "retained and explicitly incomplete"
evidence_retention = "request URL, status, headers, bounded exact body, and page identity"
telemetry = "typed page outcome and failure class"
credential_authority = "none"
robots_terms_authority = "source access preflight required"
access_control_behavior = "terminal; no alternate client or bypass"
no_bypass = true

[[policies]]
id = "CS-NET-003"
module = "constructionsight.ceqanet_csv_live_service"
capability_id = "CS-CAP-005"
methods = ["GET"]
hosts = ["ceqanet.opr.ca.gov", "ceqanet.lci.ca.gov"]
path_prefixes = ["/Search"]
query_scope = "exact SCH/document CSV export URL"
body_scope = "none"
connect_timeout_seconds = 5.0
read_timeout_seconds = 20.0
write_timeout_seconds = 5.0
pool_timeout_seconds = 5.0
max_response_bytes = 10000000
redirect_policy = "deny"
media_types = ["text/csv", "application/csv", "application/vnd.ms-excel", "text/plain"]
encodings = ["utf-8", "windows-1252"]
retryable_failures = []
terminal_failures = ["transport failure", "timeout", "redirect", "access control", "malformed CSV", "oversized response", "source drift", "retention failure"]
max_attempts = 1
backoff_seconds = 0.0
jitter = "none"
idempotency_rule = "one explicitly authorized read-only evidence request"
rate_limit = "policy-bound maximum one request per UTC day"
concurrency_limit = 1
circuit_breaker = "terminal halt on classified access-control status"
bulkhead = "one in-flight request"
cancellation = "cancellation produces no successful observation"
partial_result = "not promoted or persisted as complete evidence"
evidence_retention = "complete response envelope and exact bytes"
telemetry = "execution and independent verification identities"
credential_authority = "none"
robots_terms_authority = "current reviewed policy and source terms required"
access_control_behavior = "terminal series halt; no bypass"
no_bypass = true

[[policies]]
id = "CS-NET-004"
module = "constructionsight.parcel_source_acquisition_http"
capability_id = "CS-CAP-005"
methods = ["GET"]
hosts = ["arcgis.com", "*.arcgis.com", "*.gov"]
path_prefixes = ["/arcgis/rest/services/", "/server/rest/services/"]
query_scope = "exact capability-probe plan parameters"
body_scope = "none"
connect_timeout_seconds = 5.0
read_timeout_seconds = 30.0
write_timeout_seconds = 5.0
pool_timeout_seconds = 5.0
max_response_bytes = 5000000
redirect_policy = "deny"
media_types = ["application/json", "text/plain"]
encodings = ["utf-8"]
retryable_failures = ["transport failure", "timeout", "declared transient status", "declared ArcGIS transient service error"]
terminal_failures = ["access control", "redirect", "endpoint drift", "malformed JSON", "oversized response", "schema drift", "retention failure"]
max_attempts = 3
backoff_seconds = 1.0
jitter = "deterministic bounded plan value"
idempotency_rule = "GET-only exact request identity"
rate_limit = "plan-bound request count"
concurrency_limit = 1
circuit_breaker = "not applicable to four-request bounded proof"
bulkhead = "one source and one in-flight request"
cancellation = "abort produces explicit incomplete proof"
partial_result = "retained but never maturity-promoted"
evidence_retention = "exact response bytes and digest-bound observation"
telemetry = "typed transport/service/access/parse outcomes"
credential_authority = "false unless a later policy explicitly grants lawful credentials"
robots_terms_authority = "official public endpoint and no-bypass review required"
access_control_behavior = "terminal; no alternate endpoint or bypass"
no_bypass = true

[[policies]]
id = "CS-NET-005"
module = "constructionsight.parcel_source_bulk_rehearsal_http"
capability_id = "CS-CAP-005"
methods = ["GET"]
hosts = ["arcgis.com", "*.arcgis.com", "*.gov"]
path_prefixes = ["/arcgis/rest/services/", "/server/rest/services/"]
query_scope = "exact digest-bound count/page plan; geometry disabled"
body_scope = "none"
connect_timeout_seconds = 5.0
read_timeout_seconds = 30.0
write_timeout_seconds = 5.0
pool_timeout_seconds = 5.0
max_response_bytes = 10000000
redirect_policy = "deny"
media_types = ["application/json", "text/plain"]
encodings = ["utf-8"]
retryable_failures = ["transport failure", "timeout", "declared transient status", "declared ArcGIS transient service error"]
terminal_failures = ["access control", "redirect", "endpoint drift", "malformed JSON", "oversized response", "schema drift", "retention failure", "cancellation"]
max_attempts = 5
backoff_seconds = 1.0
jitter = "deterministic bounded plan value"
idempotency_rule = "GET-only count/page identity with single-use authorization"
rate_limit = "authorization and plan bound"
concurrency_limit = 1
circuit_breaker = "not applicable until recurring operation is separately implemented"
bulkhead = "one county rehearsal and one in-flight request"
cancellation = "terminal incomplete execution with retained evidence"
partial_result = "never bulk-authorized, imported, or promoted"
evidence_retention = "every exact count/page response, checkpoint, receipt, and portable proof bundle"
telemetry = "attempt, retry, checkpoint, resume, cancellation, and terminal classification"
credential_authority = "false"
robots_terms_authority = "official public endpoint and no-bypass review required"
access_control_behavior = "terminal; no bypass or credential substitution"
no_bypass = true

[[policies]]
id = "CS-NET-006"
module = "constructionsight.ceqanet_detail_http"
capability_id = "CS-CAP-005"
methods = ["GET"]
hosts = ["ceqanet.lci.ca.gov"]
path_prefixes = ["/"]
query_scope = "exact operator-supplied HTTPS detail URL on the approved host"
body_scope = "none"
connect_timeout_seconds = 5.0
read_timeout_seconds = 20.0
write_timeout_seconds = 5.0
pool_timeout_seconds = 5.0
max_response_bytes = 50000
redirect_policy = "deny"
media_types = ["text/html"]
encodings = ["utf-8", "windows-1252"]
retryable_failures = []
terminal_failures = ["transport failure", "timeout", "redirect", "access control", "rate limit", "terminal status", "oversized response", "media type drift", "encoding drift"]
max_attempts = 1
backoff_seconds = 0.0
jitter = "none"
idempotency_rule = "single exact read-only detail request"
rate_limit = "one explicitly consented operator request"
concurrency_limit = 1
circuit_breaker = "not applicable: one attempt"
bulkhead = "one in-flight request"
cancellation = "operator abort produces no successful snapshot"
partial_result = "bounded retained bytes remain classified as incomplete"
evidence_retention = "policy ID, request/final URL, status, content metadata, retained bytes, and failure class"
telemetry = "typed transport and policy identity"
credential_authority = "none"
robots_terms_authority = "access profile must allow public read"
access_control_behavior = "terminal; no redirect, credential, or alternate-host bypass"
no_bypass = true

[[policies]]
id = "CS-NET-007"
module = "constructionsight.source_readiness_http"
capability_id = "CS-CAP-005"
methods = ["HEAD", "GET"]
hosts = ["exact host declared by each source registry record"]
path_prefixes = ["/"]
query_scope = "the exact registry URL; GET is allowed only after HTTP 405 from HEAD"
body_scope = "none"
connect_timeout_seconds = 5.0
read_timeout_seconds = 10.0
write_timeout_seconds = 5.0
pool_timeout_seconds = 5.0
max_response_bytes = 4096
redirect_policy = "deny"
media_types = ["text/html", "text/plain", "application/json", "application/pdf", "application/octet-stream"]
encodings = ["utf-8", "windows-1252"]
retryable_failures = ["HTTP 405 method-not-allowed response authorizing one exact HEAD-to-GET fallback"]
terminal_failures = ["transport failure", "timeout", "redirect", "access control", "rate limit", "terminal status", "oversized response", "media type drift", "encoding drift"]
max_attempts = 2
backoff_seconds = 0.0
jitter = "none"
idempotency_rule = "HEAD plus one method-fallback GET only after exact 405 response"
rate_limit = "one source checked serially; at most two requests"
concurrency_limit = 1
circuit_breaker = "not applicable to bounded operator readiness run"
bulkhead = "one source and one in-flight request"
cancellation = "cancelled source remains unchecked or failed, never reachable"
partial_result = "HEAD outcome retained; fallback is explicit"
evidence_retention = "method, status, final URL, failure class, and check time"
telemetry = "typed readiness outcome"
credential_authority = "none"
robots_terms_authority = "registry URL and lawful public access only"
access_control_behavior = "terminal; no fallback after access-control response"
no_bypass = true

[[policies]]
id = "CS-NET-008"
module = "constructionsight.source_verification_http"
capability_id = "CS-CAP-005"
methods = ["GET"]
hosts = ["exact host declared by each source registry record"]
path_prefixes = ["/"]
query_scope = "exact registry URL only"
body_scope = "none"
connect_timeout_seconds = 5.0
read_timeout_seconds = 20.0
write_timeout_seconds = 5.0
pool_timeout_seconds = 5.0
max_response_bytes = 50000
redirect_policy = "deny"
media_types = ["text/html", "text/plain", "application/json", "application/xhtml+xml"]
encodings = ["utf-8", "windows-1252"]
retryable_failures = []
terminal_failures = ["transport failure", "timeout", "redirect", "access control", "rate limit", "terminal status", "oversized response", "media type drift", "encoding drift"]
max_attempts = 1
backoff_seconds = 0.0
jitter = "none"
idempotency_rule = "one exact source verification GET"
rate_limit = "one source checked serially"
concurrency_limit = 1
circuit_breaker = "not applicable to bounded manual verification"
bulkhead = "one source and one in-flight request"
cancellation = "cancellation cannot produce verified source status"
partial_result = "truncated or malformed body is not a successful verification"
evidence_retention = "policy ID, source URL, final URL, status, media type, exact bounded bytes, size, and failure class"
telemetry = "typed verification evidence and limitations"
credential_authority = "none"
robots_terms_authority = "lawful access preflight required"
access_control_behavior = "terminal; no redirect, credential, browser, or alternate-host bypass"
no_bypass = true

[[policies]]
id = "CS-NET-009"
module = "constructionsight.http_transport"
capability_id = "CS-CAP-005"
methods = ["GET", "HEAD"]
hosts = ["only the exact hosts supplied by validated operation-specific BoundedHttpPolicy objects"]
path_prefixes = ["only validated absolute operation-specific prefixes"]
query_scope = "the engine cannot execute without an immutable operation policy; caller-specific query scope remains in CS-NET-001/006/007/008"
body_scope = "none; GET and HEAD only"
connect_timeout_seconds = 5.0
read_timeout_seconds = 20.0
write_timeout_seconds = 5.0
pool_timeout_seconds = 5.0
max_response_bytes = 50000
redirect_policy = "deny"
media_types = ["operation-policy allowlist only"]
encodings = ["operation-policy allowlist only"]
retryable_failures = []
terminal_failures = ["invalid scope", "transport failure", "timeout", "redirect", "access control", "rate limit", "terminal status", "oversized response", "media type drift", "encoding drift", "malformed response"]
max_attempts = 1
backoff_seconds = 0.0
jitter = "none"
idempotency_rule = "one read-only attempt per call; no internal retry"
rate_limit = "delegated to stricter operation policy"
concurrency_limit = 1
circuit_breaker = "delegated or explicitly nonapplicable in operation policy"
bulkhead = "one synchronous in-flight request per call"
cancellation = "exception or caller cancellation produces no success"
partial_result = "bounded bytes are retained only with explicit incomplete failure classification"
evidence_retention = "immutable typed observation including policy identity and response classification"
telemetry = "no exception swallowing; stable failure kind and error type"
credential_authority = "URL credentials prohibited; no credential parameters"
robots_terms_authority = "caller must complete operation-specific lawful access review"
access_control_behavior = "terminal and returned without body-based bypass"
no_bypass = true
